rootProject.name = 'insurance-claims-event-driven'

include 'insurance-common-spring-boot-starter'
['assessment-service', 'claims-service', 'payment-service'].each { module ->
    if (new File(rootDir, module).exists()) {
        include module
    }
}
